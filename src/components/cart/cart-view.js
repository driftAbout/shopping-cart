import React from 'react';

export default class CartView extends React.Component{
  constructor(props){
    super(props);
  }

  render(){
    return (
      <section>
        <h2>Shopping Cart</h2>
      </section>
    );
  }
}